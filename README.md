# רולר 3000

Internal grid-painter tool for tracking cell-by-cell progress on a sketch, built with React + Vite.

## Development

```
npm install
npm run dev
```

## Deployment

Pushing to `main` builds the app and deploys it to GitHub Pages via `.github/workflows/deploy.yml`.
